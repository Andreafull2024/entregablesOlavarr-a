/*Escribe una función que tome dos números como parámetros y devuelva la suma de ambos.*/

function sumarNumeros(num1: number, num2: number): number {
    return num1 + num2;
}
let total = sumarNumeros(5, 3);
console.log(total);